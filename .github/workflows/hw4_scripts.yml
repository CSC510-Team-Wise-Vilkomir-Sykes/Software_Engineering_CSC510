name: HW4 Scripts
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
       
    - name: Run task 1
      if: always()
      run: |
        cd hw4/scripts
        ls
        chmod +x ./task1.sh
        ls
        bash ./task1.sh

    - name: Run task 2
      if: always()
      run: |
        cd hw4/scripts
        ls
        chmod +x ./task2.sh
        ls
        bash ./task1.sh

    - name: Run task 3
      if: always()
      run: |
        cd hw4/scripts
        ls
        chmod +x ./task3.sh
        ls
        bash ./task1.sh